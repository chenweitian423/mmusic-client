.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/

app_build/

*.apk
*.ipa
*.aab
*.iml

# pubspec.lock 必须提交进仓库(不要重新加回忽略):CI 靠它锁定依赖版本。
# 历史上 4f7cac4 那次两次构建全挂在 pub 依赖求解上
# (just_audio ^0.9.40 要 audio_session ^0.1.x,而 pubspec 写的 ^0.2.4)。
# app_build/ 整个目录已被上面的规则忽略,不需要再单列。

.DS_Store
Thumbs.db

# 随包插件目录：目录本身要存在(pubspec 里声明了它作为 asset 目录),
# 但**里面的插件不进仓库** —— 那是插件作者(公众号:科技长青)的 jsjiami 混淆产物，
# 公开仓库(=分发)不该带它。想打一个"开箱自带插件"的包，本地把 .js 丢进去再构建。
assets/plugins/*.js
